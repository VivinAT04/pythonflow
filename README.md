# PythonFlow

PythonFlow is a lightweight workflow orchestration engine inspired by Apache Airflow.

## Features

- Define Python tasks using decorators
- Create DAG dependencies between tasks
- Execute tasks in dependency order
- Retry failed tasks
- Track task status
- CLI-based workflow execution

## Example

See examples/simple_dag.py
